'use strict';

// Ensure the following is in sync with app/utils/browser-detect.js

// prettier-ignore
const browsers = [
  'safari >= 12',
  'chrome >= 100',
  'edge >= 103',
  'firefox >= 100',
  '>= 0.6%',
];

module.exports = {
  browsers,
};
