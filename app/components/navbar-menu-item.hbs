{{#if @model}}
  <LinkTo @route={{@route}} @model={{@model}} @query={{or @query (hash)}} class="dropdown-item">{{@item}}</LinkTo>
{{else}}
  <LinkTo @route={{@route}} @query={{or @query (hash)}} class="dropdown-item">{{@item}}</LinkTo>
{{/if}}
