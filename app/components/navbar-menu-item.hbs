{{#if @model~}}
  <@menu.link-to @route={{@route}} @model={{@model}} @query={{or @query (hash)}}>{{@item}}</@menu.link-to>
{{~else~}}
  <@menu.link-to @route={{@route}} @query={{or @query (hash)}}>{{@item}}</@menu.link-to>
{{~/if~}}
