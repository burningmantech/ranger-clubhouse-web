<UiButton type="submit"
          class="btn-submit"
          @type={{@type}}
          @size={{@size}}
          @onClick={{this.submitAction}}
          @responsive={{@responsive}}
          disabled={{@disabled}}
          ...attributes>
  {{this._label}}
</UiButton>
