<UiButton @onClick={{@onClick}}
          @type="gray"
          class="btn-link"
          disabled={{@disabled}}
          @responsive={{@responsive}}
          @size={{@size}}
          ...attributes>
  Cancel
</UiButton>
