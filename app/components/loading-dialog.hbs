<div class="loading-pane">
  <div class="loading-message">
    {{#if @text}}
      {{@text}}
    {{else}}
      First, do nothing. We're loading {{this.item}}.
    {{/if}}
    <div class="loading-spinner"></div>
  </div>
</div>
