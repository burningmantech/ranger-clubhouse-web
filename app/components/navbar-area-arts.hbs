<NavbarMenu @nav={{@nav}} @title="ARTs" as |menu|>
  {{#each @user.teacher.arts as |art|}}
    <NavbarMenuItem @menu={{menu}} @route="training.index" @model={{art.id}} @item={{art.title}} />
  {{/each}}
</NavbarMenu>
