<@nav.item @active={{eq @tab.activeId @paneId}}>
<a href="#{{@paneId}}"
   class="nav-link {{if (eq @tab.activeId @paneId) "active"}}"
  {{action @tab.select @paneId}}>
  {{fa-icon @icon fixed=true right=2}}
  {{yield}}
</a>
</@nav.item>
