{{#if this.isActive}}
  <ModalDialog as |Modal|>
    <Modal.title>
      Step {{this.number}} of {{this.wizard.stepCount}} - {{this.title}}
    </Modal.title>
    <Modal.body>
      {{yield}}
    </Modal.body>
    <Modal.footer class="justify-content-between">
      {{#if (not-eq this.number 1)}}
        <div>
          <UiButton @onClick={{this.wizard.backStep}} @type="secondary" @size="md">
            {{fa-icon "arrow-left" right=1}} {{this.wizard.backLabel}}
          </UiButton>
        </div>
      {{/if}}
      {{#if (or (not @isLast) @includeCancel)}}
        <div>
          <UiButton @onClick={{this.wizard.cancelAction}} @type="gray" class="btn-link" @size="md">
            Cancel
          </UiButton>
        </div>
      {{/if}}
      {{#if @isLast}}
        <div>
          <UiButton @onClick={{this.finishAction}} @size="md">
            {{this.finishLabel}}
          </UiButton>
        </div>
      {{else}}
        <div>
          <UiButton @onClick={{this.nextAction}} @type="success" @size="md">
            {{this.nextLabel}} {{fa-icon "arrow-right" left=1}}
          </UiButton>
        </div>
      {{/if}}
    </Modal.footer>
  </ModalDialog>
{{/if}}
