<section class="ui-section" ...attributes>
  {{#if (has-block "title")}}
    <div class="ui-section-title">{{yield to="title"}}</div>
  {{/if}}
  {{#if (has-block "body")}}
    {{yield to="body"}}
  {{else}}
    {{yield}}
  {{/if}}
</section>
