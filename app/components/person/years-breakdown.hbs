{{#if @person.years_as_contributor}}
  Echelon volunteer work:
  {{pluralize @person.years_as_contributor.length 'year'}} [{{year-range @person.years_as_contributor}}]<br>
  Ranger work:
  {{#if @person.years_as_ranger}}
    {{pluralize @person.years_as_ranger.length 'year'}} [{{year-range @person.years_as_ranger}}]
  {{else}}
    <i>none</i>
  {{/if}}
{{else if @person.years_as_ranger}}
  {{pluralize @person.years_as_ranger.length 'year'}} worked [{{year-range @person.years_as_ranger}}]
{{else}}
  Has not worked yet
{{/if}}
