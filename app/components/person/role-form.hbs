<ModalDialog @title="Edit Roles" @onEscape={{@onCancel}} as  |Modal|>
  <ChForm @formId="roles" @formFor={{this.roleForm}} @onSubmit={{this.save}} as |f|>
    <Modal.body>
      <div class="form-row">
        <f.input @name="roleIds" @type="checkboxGroup" @wrapClass="form-row" @options={{this.roleOptions}} />
      </div>
    </Modal.body>
    <Modal.footer @noAlign={{true}}>
      <f.submit @label="Update" @disabled={{@isSaving}} />
      <UiCancelButton @disabled={{@isSaving}} @onClick={{@onCancel}} />
      {{#if @isSaving}}
        <LoadingIndicator/>
      {{/if}}
    </Modal.footer>
  </ChForm>
</ModalDialog>
