<div class="mt-2">
  {{#if @list}}
    <b>{{pluralize @list.length @title}}</b>
  {{else}}
    No {{@title}}s
  {{/if}}
  {{#if @list}}
    <p>
      {{#if @isPosition}}
        Note: if the person is not a member of a {{@name}} eligible team, at least one shift of the following position(s)
        must be signed up for first before the person may become {{@name}} eligible.
      {{else}}
        The person is automatically {{@name}} eligible because they are a member of the following team(s):
      {{/if}}
    </p>
    <div class="columns columns-4">
      {{#each @list as |item|}}
        <div>
          {{item.title}}
        </div>
      {{/each}}
    </div>
  {{/if}}
</div>
