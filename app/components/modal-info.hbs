<ModalDialog @title={{@dialog.title}} @isInline={{true}} @modalId={{@modalId}} as |modal|>
  <modal.body>
    {{this.htmlMessage}}
  </modal.body>
  <modal.footer>
    <UiCloseButton @onClick={{@onClose}} @size="md"/>
  </modal.footer>
</ModalDialog>
