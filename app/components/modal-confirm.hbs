<ModalDialog @title={{@dialog.title}} @isInline={{@isInline}} as |modal|>
  <modal.body>
    {{this.htmlMessage}}
  </modal.body>
  <modal.footer>
    <modal.button @label="Cancel" @action={{@onClose}} @type="secondary"/>
    <modal.button @label="Confirm" @action={{@onConfirm}} @type="primary"/>
  </modal.footer>
</ModalDialog>
