<ModalDialog @title={{@dialog.title}} @isInline={{@isInline}} as |modal|>
  <modal.body>
    {{this.htmlMessage}}
  </modal.body>
  <modal.footer>
    <UiCancelButton @onClick={{@onClose}} />
    <UiButton @onClick={{@onConfirm}}>Confirm</UiButton>
  </modal.footer>
</ModalDialog>
