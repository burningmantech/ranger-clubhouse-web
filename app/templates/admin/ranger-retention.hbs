<main>
  <h1>Ranger Retention Report</h1>
  <p>
    This report shows active, inactive, inactive extension, and retired status Rangers who have worked the last 5 events
    from {{this.start_year}} up to and including {{this.end_year}}. The pandemic years, 2020 and 2021, are skipped over.
    A work year is excluded if the Ranger had only Alpha, Training, and/or Deep Freeze timesheet entries.
  </p>
  <p>
    The information is used by the Ranger Retention Team to send out retention surveys post-event.
  </p>
  <p>
    <UiExportToCSVButton @onClick={{this.exportToCsv}} />
  </p>
  <UiTable>
    <thead>
    <tr>
      <th>Callsign</th>
      <th>Status</th>
      <th>Name</th>
      <th>First Year</th>
      <th>Last Year</th>
      <th>Years Total</th>
    </tr>
    </thead>
    <tbody>
    {{#each this.people as |person|}}
      <tr>
        <td>
          <PersonLink @person={{person}} />
        </td>
        <td>{{person.status}}</td>
        <td>{{person.first_name}} {{person.last_name}}</td>
        <td>{{person.first_year}}</td>
        <td>{{person.last_year}}</td>
        <td>{{person.total_years}}</td>
      </tr>
    {{else}}
      <tr>
        <td colspan="6"><b class="text-danger">No Rangers were found? Odd.</b></td>
      </tr>
    {{/each}}
    </tbody>
  </UiTable>
</main>