<ModalDialog @title={{dialog.title}} as |modal|>
  <modal.body>
    {{{dialog.message}}}
  </modal.body>
  <modal.footer>
    <modal.button @label="Close" @action={{onClose}} @type="primary"/>
  </modal.footer>
</ModalDialog>
