{{#each workedPositions as |position|}}
{{position.title}} ({{credits-format position.credits}}, {{hour-minute-format position.duration}})
{{if (not-eq position positions.lastObject) ", "}}
{{/each}}
