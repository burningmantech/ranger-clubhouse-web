<YearSelect @title="Schedule / Sign Up" @year={{this.year}} @years={{this.person.all_years}}
            @subheader={{true}}
            @onChange={{set-value this 'year'}} />

<ScheduleManage @person={{this.person}}
                @slots={{this.slots}}
                @signedUpSlots={{this.signedUpSlots}}
                @scheduleSummary={{this.scheduleSummary}}
                @creditsEarned={{this.creditsEarned}}
                @year={{this.year}}
                @permission={{this.permission}}
                @includeLog={{true}}
                @milestones={{this.milestones}}
/>
